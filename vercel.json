{
  "buildCommand": "npm install && npm install zod && npm install -D tailwindcss postcss autoprefixer && npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "outputDirectory": ".next"
} 